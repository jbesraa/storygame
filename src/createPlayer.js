const generatePlayerLogo = () => {
  return;
};

const generatePlayerName = () => {
  return;
};

const createPlayer = () => {
  const name = generatePlayerName();
  const logo = generatePlayerLogo();

  return {
    name,
    logo,
  };
};

export default createPlayer;
